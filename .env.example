# Server
PORT=3000
NODE_ENV=development
APP_BASE_URL=http://localhost:3000
SESSION_COOKIE_NAME=volynx_session
JWT_SECRET=CHANGE_ME_SUPER_LONG_RANDOM
# Public scanning page: if false, /q/:token only shows "restricted"
PUBLIC_TOKEN_STATUS=false
# Storage
DATA_DIR=./data
